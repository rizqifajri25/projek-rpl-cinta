import { buildDirectionUrl, palembangCenter } from '../../lib/maps';
export default function GoogleMapPanel({places=[],selected}){const target=selected?{lat:selected.latitude,lng:selected.longitude}:palembangCenter;return <section className='card grid gap-3'><div className='h-72 rounded-xl bg-orange-100 grid place-items-center'><div className='text-center'><p className='font-bold'>Google Maps Marker & Nearby Culinary</p><p>{target.lat}, {target.lng}</p><p>{places.length} kuliner terdekat</p></div></div>{selected&&<a className='btn text-center' href={buildDirectionUrl({lat:selected.latitude,lng:selected.longitude})} target='_blank'>Direction & Route Navigation</a>}</section>}
