import { useEffect,useState } from 'react';import { api } from '../../lib/api';import CulinaryCard from '../../components/CulinaryCard';import StateBlock from '../../components/ui/StateBlock';import GoogleMapPanel from '../../components/maps/GoogleMapPanel';export default function ExplorePage(){const[items,setItems]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{api.get('/culinary').then(r=>setItems(r.data.data||r.data)).catch(()=>setError('kuliner tidak dapat dimuat')).finally(()=>setLoading(false))},[]);return <StateBlock loading={loading} error={error} empty={!items.length}><div className='grid gap-4'><h1 className='text-3xl font-black'>Explore Kuliner</h1><GoogleMapPanel places={items}/><div className='grid md:grid-cols-3 gap-4'>{items.map(p=><CulinaryCard key={p.id} place={p}/>)}</div></div></StateBlock>}
