import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import { api } from '../../lib/api';import GoogleMapPanel from '../../components/maps/GoogleMapPanel';import ReservationForm from '../../components/forms/ReservationForm';import StateBlock from '../../components/ui/StateBlock';export default function CulinaryDetailPage(){const{slug}=useParams();const[p,setP]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/culinary/'+slug).then(r=>setP(r.data)).finally(()=>setLoading(false))},[slug]);return <StateBlock loading={loading} empty={!p}>{p&&<div className='grid md:grid-cols-2 gap-4'><section className='card'><span className='badge'>{p.halal_status}</span><h1 className='text-4xl font-black'>{p.name}</h1><p>{p.description}</p><p>{p.address}</p><p>⭐ {p.rating_average} dari {p.rating_total} review</p></section><GoogleMapPanel selected={p}/><ReservationForm culinaryPlaceId={p.id}/></div>}</StateBlock>}
