import { useState } from 'react';import { api } from '../../lib/api';export default function LoginPage(){const[f,setF]=useState({email:'',password:''});async function submit(e){e.preventDefault();const r=await api.post('/login',f);localStorage.setItem('smwkp_token',r.data.token)}return <form onSubmit={submit} className='card grid gap-3 max-w-md mx-auto'><h1 className='text-2xl font-bold'>Login</h1><input className='input' placeholder='Email' onChange={e=>setF({...f,email:e.target.value})}/><input className='input' type='password' placeholder='Password' onChange={e=>setF({...f,password:e.target.value})}/><button className='btn'>Login</button></form>}
